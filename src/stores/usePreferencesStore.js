import { defineStore } from 'pinia'

export const usePreferencesStore = defineStore('preferences', {
  state: () => ({
    // Will be completed in subsequent stories
  }),
  actions: {},
  getters: {}
})
